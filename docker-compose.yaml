services:
  db:
    container_name: postgresql_container
    image: postgres:14.10
    env_file:
      - .env
    environment:
      - POSTGRES_DB=${ELEPHANT_DB}
      - POSTGRES_PASSWORD=${ELEPHANT_DB_USERNAME}
      - POSTGRES_USER=${ELEPHANT_DB_PASSWORD}
    volumes:
      - ./postgres-data:/var/lib/postgresql@14/data
    restart: always
  backend:
    build: .
    image: ghcr.io/stickydice/midas-server-ghcr
    container_name: server_container
    ports:
      - 8181:8181
    env_file:
      - .env
    environment:
      - spring.datasource.url=${ELEPHANT_DB_URL}
      - spring.datasource.username=${ELEPHANT_DB_USERNAME}
      - spring.datasource.password=${ELEPHANT_DB_PASSWORD}
      - spring.datasource.hikari.minimumIdle = 2
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
    depends_on:
      - db
    restart: always